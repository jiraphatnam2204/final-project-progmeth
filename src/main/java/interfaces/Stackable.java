package interfaces;

/**
 * Represents an item that can be stacked in the player's inventory.
 */
public interface Stackable {

    /** The default maximum stack size for stackable items. */
    int maxStack = 30;
}
